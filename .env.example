# Example environment variables
# Use GEMINI_API_KEY for the server-side Gemini key (recommended)
GEMINI_API_KEY=your_gemini_api_key_here

# For local development we return a friendly message if the key is missing.
